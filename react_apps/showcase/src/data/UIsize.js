const au = 50
const headerHeight= 1.25 * au
const carouselHeight = 8 * au
const cardHeight = 7 * au
const UIgap = 0.1 * au
const UIgapL = UIgap * 2
const UIgapXL = UIgap * 4
const radius = 0.1 * au
const radiusL = 0.2 * au
const radiusXL = 0.4 * au

export const UIsizes = {
    headerH:headerHeight,
    carouselH:carouselHeight,
    logoH:au,
    gap:UIgap,
    gapL:UIgapL,
    gapXL:UIgapXL,
    rad:radius,
    radL:radiusL,
    radXL:radiusXL,
}